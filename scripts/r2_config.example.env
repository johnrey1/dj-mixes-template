# Copy this file to .env in the repo root and fill in real values.
# NEVER commit the real .env — it's already excluded via .gitignore.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_URL=
